version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "env"
      - run:
          name: "Say hello again"
          command: "git rev-parse HEAD^"
          when: always

workflows:
  say-hello-workflow:
    jobs:
      - say-hello